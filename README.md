# Restaurant

object oriented programming
1. encapsulation - information hiding
2. inheritance - classes can inheriet behaviors from other class (tostring inherited by restaurant class from object class)
3. polymorphism - same method can be called by different classes and used in different ways depending on what calls it, overriding and overloading

## Added new property
- In addition to previous properties, added a name variable for restaurants

## JOptionpane
- Learned how to import JOptionpane, and save space by not importing full package, for collecting user input
- Create and call method of JOptionpane, to gather user input about restaurants and store it in a variable

## Getters & Setters
- Learned what getters and setters are and created them for each variable in the Restaurant class
  - Allowing to make all Restaurant class variables private but still changeable from the main method

## to.String()
- Use the to.String() function to define how calling a restaurant looks when it is printed to the console
- to.String() shows each variable of the Restaurant printed with the value given by the user input from the JOptionpane

## Array List
- Imported and instantiated an array list
